# תכנון POC — הוכחת היתכנות לשני מסכים סינכרוניים

> **סטטוס:** טיוטה לאישור
> **מסמך התכנון המלא:** `PLAN.md` בריפו `trivia-event-system`

## מטרת ה-POC

להוכיח את הבסיס הטכני של המערכת לפני בניית המערכת המלאה. ה-POC **אינו** המשחק — הוא בודק רק את הסיכונים הטכניים:

1. **סנכרון דו-מסכי** — חלון אדמין וחלון תצוגה על אותו מחשב, מסונכרנים מיידית דרך BroadcastChannel.
2. **טיימר מסונכרן** — שעון עצר 2:00 שרץ בשני המסכים יחד, עם השהיה/המשך, וחסימת כפתורים בזמן השהיה.
3. **ניקוד בזמן אמת** — כפתורי נכון/שגוי ופאנל ±1 ידני, עם עדכון מיידי ואנימציה במסך הקהל.
4. **Undo** — כפתור "חזור" שמבטל את הפעולה האחרונה כולל ניקוד.
5. **שרידות** — רענון של כל אחד מהחלונות (או שניהם) מחזיר בדיוק לאותו מצב, כולל זמן שנותר בטיימר.
6. **עברית RTL** — כל הממשק בעברית, כיווניות נכונה.
7. **עטיפת אלקטרון** *(נוסף בהחלטה מ-2026-07-25)* — המשתמש הסופי אינו טכני, ולכן המוצר ייארז כאפליקציית שולחן-עבודה: לחיצה כפולה פותחת אוטומטית חלון אדמין על המסך הראשי וחלון קהל (מסך מלא) על המסך המשני. ה-POC מוכיח שהסנכרון והשמירה עובדים גם בתוך אלקטרון.

## מה יש ב-POC

- **`/admin`:** רשימת 5 שחקני דמו, שאלה נוכחית (מתוך ~10 שאלות דמו קבועות בקוד), שם השחקן שבתורו, כפתורי נכון/שגוי, טיימר עם התחל/השהה/המשך/איפוס, פאנל ±1, כפתור "חזור", כפתור "איפוס".
- **`/display`:** השאלה בפונט גדול, טבלת שחקנים עם השחקן הפעיל מודגש, טיימר גדול, חיווי "הפסקה" בהשהיה, אנימציית עדכון ניקוד.

## מה אין ב-POC (בכוונה)

- אין מסכי עריכת תוכן — שאלות ושחקני דמו קבועים בקוד (hardcoded). ניהול התוכן המלא נבנה רק במערכת האמיתית.
- אין 4 שלבי משחק, אין חלוקה לזוגות, אין תמונות, אין מסך ניצחון.
- עיצוב בסיסי בלבד — הפקת העיצוב המלא (סגנון הלוגו) נעשית במערכת האמיתית.

## טכנולוגיה

React + Vite + TypeScript, Zustand + reducer עם היסטוריית snapshots ל-Undo, BroadcastChannel לסנכרון (אדמין = מקור אמת, תצוגה = ראי + SYNC_REQUEST), IndexedDB לשרידות.

**אלקטרון:** תהליך ראשי (`electron/main.cjs`) שמרים שרת סטטי פנימי על `127.0.0.1` בפורט **קבוע** ופותח את שני החלונות ממנו. הפורט הקבוע קריטי — IndexedDB נשמר לפי origin (כתובת+פורט), ופורט משתנה בין הפעלות היה מוחק את המצב השמור. הגשה מ-localhost מבטיחה ש-BroadcastChannel עובד בין החלונות בדיוק כמו בדפדפן.

## קריטריון הצלחה

הרצה מקומית: פתיחת שני חלונות (`/admin` + `/display`), הרצת "מיני-סבב" — התחלת טיימר, כמה תשובות נכון/שגוי, ±1 ידני, השהיה, Undo, רענון שני החלונות באמצע — והכל ממשיך בדיוק מאותה נקודה.
